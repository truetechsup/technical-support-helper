#!/bin/bash
# Скрипт для деплоя через Git (запускать на сервере)

# Настройки
PROJECT_DIR="~/errors/Errors"
GIT_REPO="your-github-repo-url"  # Измените на ваш репозиторий

# Цвета
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
NC='\033[0m'

echo -e "${YELLOW}🔄 Обновление из Git...${NC}"

cd "$PROJECT_DIR" || exit 1

# Обновляем код из Git
git pull origin main

if [ $? -ne 0 ]; then
    echo "❌ Ошибка при обновлении из Git"
    exit 1
fi

# Перезапускаем контейнеры
echo -e "${YELLOW}🔄 Перезапуск контейнеров...${NC}"
docker compose down
docker compose up -d --build

echo -e "${GREEN}✅ Приложение обновлено!${NC}"

